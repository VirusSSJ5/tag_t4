#ifndef COMMONHPP
#define COMMONHPP

#include <array>
#include <string>
#include <map>
#include <iostream>
#include <fstream>

using std::array;
using std::string;
using std::pair;
using std::map;
using std::cout;
using std::endl;
using std::ifstream;
using std::getline;

using uint=unsigned int;

using ii=pair<int,int>;

#define FOR(i,max)		for(int i=0;	i<(max);i++)
#define FOR2(i,min,max) for(int i=(min);i<(max);i++)

#endif//COMMONHPP
